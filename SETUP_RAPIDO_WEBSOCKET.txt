╔════════════════════════════════════════════════════════════════╗
║                                                                ║
║  ⚡ REPARACIÓN WEBSOCKET - GUÍA RÁPIDA                        ║
║  Tiempo estimado: 5 minutos                                   ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝

📍 UBICACIÓN: /var/www/mundoindustrial (en el servidor)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 1: Descargar/Copia los archivos necesarios

Los siguientes archivos ya están listos en el proyecto:
  ✓ instalar-reverb.sh     → Script de instalación
  ✓ diagnostico-reverb.sh  → Script de diagnóstico
  ✓ reverb.conf            → Configuración de Supervisor
  ✓ .env                   → Ya actualizado

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 2: Conectarse al servidor VPS (en terminal)

```bash
ssh root@sistemamundoindustrial.online
# O si tienes IP:
ssh root@[IP_DEL_SERVIDOR]
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 3: Hacer scripts ejecutables

```bash
cd /var/www/mundoindustrial
chmod +x instalar-reverb.sh
chmod +x diagnostico-reverb.sh
chmod +x iniciar-reverb.sh
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 4: EJECUTAR INSTALACIÓN (lo más importante)

```bash
sudo ./instalar-reverb.sh
```

⏰ Espera a que termine (2-3 minutos)

Deberías ver:
  ✓ Reverb instalado
  ✓ Supervisor configurado
  ✓ Reverb iniciado (RUNNING)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 5: Verificar que funciona

```bash
# Ver estado
supervisorctl status reverb

# Deberías ver: reverb                    RUNNING
```

```bash
# Ver logs
tail /var/log/mundo-industrial/reverb.log

# Deberías ver: Reverb started successfully
```

```bash
# Verificar puerto
netstat -tln | grep 8080

# Deberías ver: tcp  0  0 0.0.0.0:8080  0.0.0.0:*  LISTEN
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 6: Actualizar Nginx (si es necesario)

Si quieres WSS (WebSocket Secure), verifica que Nginx
esté configurado correctamente:

```bash
# Revisar configuración actual
cat /etc/nginx/sites-enabled/sistemamundoindustrial.online | grep -A 10 "location /app"
```

Si NO tiene la configuración de WebSocket, copia esto:

```bash
# Copiar configuración recomendada
cp nginx-reverb-config.txt /tmp/
cat /tmp/nginx-reverb-config.txt
```

Y agrégalo a tu configuración de Nginx.

Luego recarga Nginx:
```bash
sudo nginx -t  # Probar sintaxis
sudo systemctl reload nginx
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔹 PASO 7: Acceder a la página web y verificar

1. Abre el navegador
2. Ve a: https://sistemamundoindustrial.online
3. Abre la consola (F12)
4. Busca estos mensajes:

  ✅ SI funciona:
     "✅ WebSocket conectado exitosamente a Reverb"
     
  ❌ SI NO funciona:
     "❌ WebSocket connection failed"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ SOLUCIÓN DE PROBLEMAS

Si sigue sin funcionar, ejecuta:

```bash
sudo /var/www/mundoindustrial/diagnostico-reverb.sh
```

Esto te dirá exactamente qué está mal y cómo arreglarlo.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 COMANDOS RÁPIDOS DE REFERENCIA

# Ver estado Reverb
sudo supervisorctl status reverb

# Ver logs
sudo tail -f /var/log/mundo-industrial/reverb.log

# Reiniciar Reverb
sudo supervisorctl restart reverb

# Detener Reverb
sudo supervisorctl stop reverb

# Iniciar Reverb
sudo supervisorctl start reverb

# Diagnóstico completo
sudo /var/www/mundoindustrial/diagnostico-reverb.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ ¡ESO ES! El WebSocket en tiempo real debería funcionar ahora.

Si tienes problemas, revisa:
  1. Logs: /var/log/mundo-industrial/reverb.log
  2. Estado: supervisorctl status reverb
  3. Puerto: netstat -tln | grep 8080
  4. Firewall: ufw status

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

